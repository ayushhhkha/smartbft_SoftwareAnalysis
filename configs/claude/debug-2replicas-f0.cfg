\* Full-featureset check of BFTSMaRtTotalOrder on the debug-2replicas-f0 setup.
\* Same constants as configs/debug-2replicas-f0.cfg (SmartBFT parity); adds the
\* multi-instance safety invariants, Values symmetry, and liveness.
\* Run against ClaudeSmartBFT.tla (NOT SmartBFT.tla).

SPECIFICATION FairSpec

CONSTANTS
    Replicas = {1,2}
    Values = {v1,v2}
    MaxConsensus = 2
    F = 0
    Faulty = {}
    Leader = 1
    NoValue = NoValue

\* Permutations(Values); safe here because the asserted liveness genuinely holds.
SYMMETRY Symmetry

INVARIANTS
    TypeOK
    Agreement
    Validity
    Integrity
    AcceptImpliesWrite
    OrderedDelivery
    DecisionMatchesAccept
    CertificateUniqueness
    TotalOrder
    PrefixGapFree

PROPERTIES
    Termination
