\* Mutation probe: f+1 quorum + Byzantine, equivocating leader (r3).
\* Expectation: Agreement / CertificateUniqueness are VIOLATED, proving the
\* properties have teeth and that the 2f+1 threshold is necessary.
CONSTANTS
    Replicas  = {0, 1, 2, 3}
    Byzantine = {3}
    Leader    = 3
    Values    = {v1, v2}
    F         = 1
    NoVal     = NoVal

\* Override the quorum threshold to the (incorrect) weak value.
Quorum <- WeakQuorum

INIT Init
NEXT Next

INVARIANT Agreement
INVARIANT CertificateUniqueness
